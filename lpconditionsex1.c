#include <stdio.h>

// Write a program that displays all even numbers from 1 to 20.

int main(void) {
    
    int i = 0;
    
    while (i < 20) {
	    i++;
	    if (i % 2 == 0) {
                    printf("%d\n", i);
	    }
	    else {
	    }
    }
    return 0;
}
     
